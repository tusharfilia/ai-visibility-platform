FROM node:22-alpine

# Set working directory
WORKDIR /app

# Copy just the standalone server
COPY apps/api/src/main-standalone.js .

# Expose port
EXPOSE 8080

# Start the standalone server
CMD ["node", "main-standalone.js"]
