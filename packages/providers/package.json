{
  "name": "@ai-visibility/providers",
  "version": "1.0.0",
  "description": "AI search engine providers with mock implementations and fixtures",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@ai-visibility/shared": "workspace:*",
    "openai": "^4.20.0",
    "@anthropic-ai/sdk": "^0.24.0",
    "@google/generative-ai": "^0.2.0",
    "@azure/openai": "^2.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  }
}
